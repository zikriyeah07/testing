# testing

This workspace contains a minimal Hello World HTML page.

How to open:

- Open [index.html](index.html) in your browser.
- Or run a simple local server (Python):

```bash
python -m http.server 8000
# then open http://localhost:8000
```

The page logs "Hello, world!" to the console and shows an alert when the button is clicked.